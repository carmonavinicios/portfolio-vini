:root{
  --cor-fundo:#A1170D;
  --cor-destaque:#000000;
   --cor-texto:#FFFFFF;
}
*{
  margin: 0;
  padding:0;
}
body{
  background-color: var(--cor-fundo);
  color: var(--cor-texto);
}
strong{
  color:var(--cor-destaque);
}
.apresentacao{
  display: flex;
  margin: 10%;
}
a{
  
  
}
.foto
}